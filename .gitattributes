# The CI fixture and workflows are not part of the distributed package.
# The config templates themselves must stay, since consumers copy them
# out of vendor/bear/qatools/.
/smoke           export-ignore
/.github         export-ignore
/.gitattributes  export-ignore
/.gitignore      export-ignore
