<?xml version="1.0"?>
<psalm
    errorLevel="1"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config https://psalm.dev/schema/config"
>
    <projectFiles>
        <directory name="src" />
        <ignoreFiles>
            <directory name="vendor" />
        </ignoreFiles>
    </projectFiles>

    <issueHandlers>
        <!--
            Only src is analysed, by design: errorLevel 1 is too strict for test
            code, where ClassMustBeFinal and similar rules work against normal
            PHPUnit practice. The trade-off is that unused-class detection has
            nothing to go on. A class reached only through the framework - every
            ResourceObject, resolved by URI rather than referenced from src - or
            used only from tests is reported as never used. Since neither case
            is a defect, the check is switched off rather than left to be
            silenced class by class with @psalm-api.
        -->
        <UnusedClass errorLevel="suppress"/>
    </issueHandlers>
</psalm>
